# Never commit customer secrets
customers/*/.env

# Shared database stack secrets
database/.env

# GHCR pull token — created manually on the server
.ghcr-token

# Traefik stores certs here at runtime
traefik/acme.json
